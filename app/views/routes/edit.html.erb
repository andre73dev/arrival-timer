<%= form_for(@route) do |f| %>
  <%= render 'shared/error_messages', object: f.object %>
  <div class="field">
    <%= f.text_area :destination, placeholder: "目的地:東京駅等..." %>
  </div>
  <%= f.submit "いまいく", class: "btn btn-primary" %>
<% end %>

